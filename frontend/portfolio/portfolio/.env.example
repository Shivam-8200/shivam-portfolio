# Gemini API key for the "Ask AI about me" chat window (server-side only, never exposed to browser)
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# URL of the Express contact backend (see /portfolio-backend)
NEXT_PUBLIC_API_URL=http://localhost:8787
